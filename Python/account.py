class Account:
    def __init__(self, name, document):
        self.id = int()
        self.name = str(name)
        self.document = str(document)
        self.email = str()
        self.password = str()
